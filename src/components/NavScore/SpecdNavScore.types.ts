/**
 * Props for the SpecdNavScore (`<specd-nav-score>`) component.
 *
 * @example
 * <specd-nav-score score="87"></specd-nav-score>
 */
export interface NavScoreProps {
  /** Numeric score string displayed in the badge (e.g. "87"). */
  score?: string;
}
