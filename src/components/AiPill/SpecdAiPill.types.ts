/**
 * Props for the SpecdAiPill (`<specd-ai-pill>`) component.
 *
 * @example
 * <specd-ai-pill label="Fix with AI"></specd-ai-pill>
 */
export interface AiPillProps {
  /** Text label displayed next to the sparkle icon. @default 'Fix with AI' */
  label?: string;
}
