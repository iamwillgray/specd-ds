/**
 * Props for the SpecdJumpBtn (`<specd-jump-btn>`) component.
 *
 * @example
 * <specd-jump-btn label="Jump to layer"></specd-jump-btn>
 */
export interface JumpBtnProps {
  /** Button label. @default 'Jump to layer' */
  label?: string;
}
